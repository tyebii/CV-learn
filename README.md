Program is set up to run all transformation functions and produce an image for each function. 
Input and output file paths must be specified by creating a 'config.c' file using the format defined in config.h file.
Inputting only the filename as the filepath will place the resulting image in the project root directory by default.
